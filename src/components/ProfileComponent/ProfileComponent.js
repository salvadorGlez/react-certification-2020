import React from 'react';
import { Profile } from './styles';

export const ProfileComponent = () => {

    return (
        <Profile className="section user__section">
            <button className="btn login__button">
                Log in
            </button>
        </Profile>
    );
}
